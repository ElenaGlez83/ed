package veterinario;

/**
 * Clase Veterinario desde la que controlaremos todo lo que queramos hacer con
 * la aplicación, como muestra métodos de alta de clientes y mascotas.
 * 
 * @author Elena González Rojo
 */
public class Veterinario {

    // Definimos algunos de los métodos que podrían estar definidos en esta clase
    /**
     * Método que dará de alta un nuevo cliente en el veterinario, creando un
     * array de mayor dimensión si es necesario.
     *
     * @param cliente nuevo cliente del veterinario
     * @return true si se ha podido añadir el nuevo cliente, false en caso
     * contrario
     */
    public boolean altaCliente(Cliente cliente) {
        return true;
    }

    /**
     * Método que dará de alta una nueva mascota en el veterinario, creando un
     * array de mayor dimensión si es necesario.
     *
     * @param mascota nueva mascota del veterinario
     * @return true si se ha podido añadir la nueva mascota, false en caso
     * contrario
     */
    public boolean altaMascota(Mascota mascota) {
        return true;
    }
}
